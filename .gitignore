node_modules/
.next/
.data/
.env*.local
anchor/target/
anchor/.surfpool/
.agents/
.vercel
.env*

# Screen recordings dropped in for reference, not source.
*.mov

# Worker state: an audit trail, but a local one
backend/data/
backend/node_modules/
backend/.env

# Vanity mint keypairs. These are the authority-less mint addresses for the
# share tokens; losing them before launch means regrinding, and committing them
# means anyone can front-run creating the tracker at that address.
.keys/
